<Window x:Class="FinalcoverAssignment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalcoverAssignment"
        mc:Ignorable="d"
        Title="MainWindow" Height="100" Width="300">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Button to open a file dialog and select file  -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="5,0,0,0">
            <Button x:Name="btnSel" Margin="0,15,0,0" Click="FileSelect">Select File</Button>
        </StackPanel>
        <!-- Exits the program  -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <Button x:Name="btnExit" Margin="0,15,0,0" Click="ButtonExit">Exit</Button>
        </StackPanel>

    </Grid>
</Window>
